# Digit Models

Run `python scripts/download_digit_models.py` from the repository root to download:

- `mnist-12.onnx`
- `mnist-12-int8.onnx`

Both files come from the official ONNX Model Zoo MNIST directory and are stored locally so Phase 1 can run offline after setup.

